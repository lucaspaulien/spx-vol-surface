"""volsurface: SVI & Heston implied volatility surface calibration on SPX options.

Pure numpy/pandas core (no hard scipy dependency) so the calibration logic is
transparent and testable end-to-end without a network connection or a heavy
dependency stack. See README.md for methodology and results.
"""

__version__ = "0.1.0"
